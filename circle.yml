machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t douglasralmeida/data.bhsul .

test: 
  override:
    - docker run -d --name databhsul -e MYSQL_ROOT_PASSWORD=senhateste -p 33060:3306 douglasralmeida/data.bhsul; sleep 10
    - docker logs databhsul
    - mysql --user=root --password=senhateste --port=33060 --execute="SELECT User, Host FROM mysql.user"
    - mysql --user=usuario_simples --password="$SQL_PASSWORD" --port=33060 --execute="SELECT * FROM bhsul_gestaosite"

deployment:
  hub:
    branch: master
    commands:
      - docker login -e douglasralmeida@live.com -u douglasralmeida -p $DOCKER_PASS
      - docker push douglasralmeida/data.bhsul